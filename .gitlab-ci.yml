image: docker:stable

stages:
- build
- test
- deploy

build-project:
    services:
    - docker:dind
    variables:
        GIT_DEPTH: "1000000"
    stage: build
    script:
    - ls
    - dotnet build