image: docker:stable

stages:
- build
- test
- deploy

build-project:
    image: mcr.microsoft.com/dotnet/sdk:3.1
    services:
    - docker:dind
    variables:
        GIT_DEPTH: "1000000"
    stage: build
    script:
    - ls
    - dotnet build